import { PrimaryCard } from '@attom';

export const Component = ({
	// box Control
	boxProps,
}) => {
	return (
		<PrimaryCard boxProps={boxProps} rounded>
			Component
		</PrimaryCard>
	);
};

export default Component;
